name = "python-learning-api"
main = "src/index.js"
compatibility_date = "2023-12-01"

# 这是 Workers 的配置文件
# 部署时需要根据您的账户信息修改以下内容：
# account_id = "your-account-id"  # 在Cloudflare Dashboard中找到
# 在部署时，Cloudflare会自动配置路由